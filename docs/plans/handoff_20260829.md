# 交接文件 — 2026-08-29

## 現況

InMind 測驗（`/api/report`）回報「出了點小狀況／網路或 AI 服務暫時有問題」。

**不是 SDK 版本問題**（那次災情已修好，`curl https://mindgym-api-aj7z.onrender.com/healthz`
可查線上實際版本，公開免登入）。

**真正原因**：這次併進主分支的訂閱功能新增了 `_check_baseline_assessment_quota()`
（[backend/app.py:248](../../backend/app.py:248)）——免費層基線檢測限 1 次，第二次
起後端回 HTTP 402。前端原本沒處理 402，一律顯示成「服務有問題」，使用者重試也沒用。

已在本 PR 修正：[src/routes/onboarding.tsx](../../src/routes/onboarding.tsx) 攔下
402 並改開付費牆（`PaywallScreen`），保留使用者已填的作答。

## 待拍板的產品決策

**現在只要做過一次測驗的既有使用者，全部會撞這道牆**（免費層不能重測）。這是不是
預期的上線行為，需要有人決定：
- 是 → 維持現狀即可，本 PR 修正已經足夠
- 否 → 需要另外討論白名單／過渡期方案

## 部署機制（不需要 Render 密碼也能做的事）

- **push main = Vercel 前端 + Render 後端自動部署到正式站**（`docs/plans/pro_modules_plan.md:47`）
- repo 沒有 `render.yaml`，build/start 指令設在 Render 後台，但**改程式碼不必碰後台**
- main 是保護分支，一律走 PR merge 觸發部署，**不要直接 push main**
- `supabase/*.sql` 不會自動部署，要手動在 Supabase SQL editor 跑。`supabase/subscriptions.sql`
  已確認跑過（正式 DB 已有 `subscriptions` 表）

## 只有 Render 後台權限才能做的事

這幾件事目前**沒有人有權限做**，是實際風險：

1. **環境變數**：`ANTHROPIC_API_KEY`、`SUPABASE_KEY`、`SUPABASE_URL`。金鑰過期/輪替卡在這裡。
2. **後端 log**：`perma_scores` 寫入失敗只會 `logger.error`，被 `except` 吞掉
   （[backend/app.py:872](../../backend/app.py:872)）——如果測驗結果開始寫不進資料庫，
   畫面完全正常，唯一看得到的地方是 Render log。
3. **手動 redeploy / 清 build cache**。
4. **改 build/start 指令**。

建議：用 Render 的「邀請成員」把接手的人加進 team，不要共用帳密。

## 待交接的其他權限

Render、Vercel、Supabase、GitHub repo、Apple Developer（App Store 退件重送審進行中，
見 [appstore_rejection_20260816_response.md](appstore_rejection_20260816_response.md)）。
